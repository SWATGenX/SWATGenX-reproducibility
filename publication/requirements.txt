# Python environment for reproducing publication tables and figures.
# Verified on Python 3.10.12 (Linux). Geospatial stack mirrors the production build.
# Install:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
geopandas==1.0.1
fiona==1.10.1
shapely==2.0.6
pyproj==3.7.0
pandas==2.2.3
numpy==2.2.3
matplotlib==3.9.2
pypdf==5.4.0
