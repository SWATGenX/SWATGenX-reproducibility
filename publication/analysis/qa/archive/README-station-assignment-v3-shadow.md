# Archived: station assignment v3 shadow inventory (closed 2026-05-31)

Per-model shadow outputs from the v3 portfolio comparison run live here. **Shadow inventory is closed.**

- **Production method:** `SWATGenX/station_assignment_v3.py` via `fetch_streamflow_for_watershed()` in `SWATplus_streamflow.py`.
- **Published inventory:** `publication/analysis/qa/station-assignment-v3*-inventory-*.csv` (regenerated by `finalize_station_assignment_v3_inventory.py`).
- **Do not** re-open shadow runs for routine updates; refresh inventory from production `streamflow_data/stations_assignment_v3.csv` when models are rebuilt.

## Contents

| Directory | Suite |
|-----------|--------|
| `station-assignment-v3-shadow-roster-20260531/` | 8 evaluation models (`tab-model-roster.csv`) |
| `station-assignment-v3-shadow-showcase-20260531/` | Showcase disk inventory (`example-models-inventory.csv`) |

Legacy assignment scripts (`peace_nhd_first_pick` phase runners, pre-v3 portfolio replays) remain in `publication/analysis/scripts/` for reference only.
